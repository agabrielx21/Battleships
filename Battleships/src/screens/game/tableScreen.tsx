import React from 'react';
import { GameContext } from '../../hooks/gameContext';

const TableScreen = () => {
    return (
        <>
        </>
    )
};

export default () => (
    <GameContext>
        <TableScreen/>
    </GameContext>
);
